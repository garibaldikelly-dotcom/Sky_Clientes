name: 🆕- Alta Nuevo Cliente

on:
  workflow_dispatch:
    inputs:
      cliente_id:
        description: 'ID del Cliente a crear (Simulación)'
        required: true
        default: 'NEW-001'

jobs:
  simular_proceso:
    runs-on: ubuntu-latest
    steps:
      - name: 💬 Simular Reacción - Asignación a Back-end (Manual)
        run: |
          echo "========================================================================"
          echo "::notice file=PLANIFICACION.md,title=Asignación (Cliente Nuevo - Manual)::Solicitud de Nuevo Cliente disparada manualmente."
          echo "::warning title=ACCIÓN REQUERIDA (EQUIPO BACKEND)::Proceso de creación de cliente iniciado para ID: ${{ github.event.inputs.cliente_id }}. Revisar el proceso de alta en el sistema."
          echo "Disparado por: ${{ github.actor }}"
          echo "Fecha de Ejecución: $(date)"
          echo "========================================================================"